{
    "properties": {
        "samples": {
            "default": "",
            "title": "Sample IDs (comma or space separated)",
            "type": "string"
        },
        "assay": {
            "default": "exome",
            "title": "Assay to run",
            "type": "string",
            "enum": ["exome", "cdl", "exome-panel", "exome-pod", "neurodegenerative-nanoxome", "exome-dev"]
        },
        "gene_list": {
            "default": "",
            "title": "Gene list or POD code (required if assay is exome-panel or exome-pod)",
            "type": "string"
        },
        "sample_base": {
            "default": "s3://ncgl-prod.sample-bucket",
            "title": "Base S3 path to sample FASTQs",
            "type": "string"
        },
        "output_base": {
            "default": "s3://ncgl-prod.sample-bucket",
            "title": "Base S3 path for output",
            "type": "string"
        }
    },
    "required": [
        "samples", 
        "assay",
        "sample_base",
        "output_base"
    ],
    "type": "object"
}