{
    "properties": {
        "samples": {
            "default": "20-10508-1",
            "title": "Sample IDs (comma or space separated)",
            "type": "string"
        },
        "assay": {
            "default": "cdl",
            "title": "",
            "type": "string"
        },
        "sample_base": {
            "default": "s3://ncgl-prod.sample-bucket",
            "title": "Base S3 path to sample FASTQs",
            "type": "string"
        },
        "output_base": {
            "default": "s3://uwlm-personal/nkrumm/cdl_baseline/output",
            "title": "Base S3 path for output",
            "type": "string"
        }
    },
    "required": [
        "samples", 
        "assay",
        "sample_base",
        "output_base"
    ],
    "type": "object"
}