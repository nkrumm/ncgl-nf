{
    "properties": {
        "samples": {
            "default": "",
            "title": "Sample IDs (comma or space separated)",
            "type": "string"
        },
        "assay": {
            
        },
        "assay": {
            "default": "exome",
            "title": "Assay to run",
            "type": "string",
            "enum": ["exome", "cdl", "exome-panel", "neurodegenerative-nanoxome"]
        },
        "gene_list": {
            "default": "",
            "title": "Gene list (required if assay is exome-panel)",
            "type": "string"
        },
        "sample_base": {
            "default": "s3://ncgl-prod.sample-bucket",
            "title": "Base S3 path to sample FASTQs",
            "type": "string"
        },
        "output_base": {
            "default": "s3://ncgl-prod.sample-bucket",
            "title": "Base S3 path for output",
            "type": "string"
        }
    },
    "required": [
        "samples", 
        "assay",
        "sample_base",
        "output_base"
    ],
    "type": "object"
}